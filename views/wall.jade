extends layout

block scripts
  != js('wall')
  script(type="text/javascript")
    var directory = !{JSON.stringify(directory)};
  
block content
  header
    h1 Global Hackerspaces Status Wall
    section #{total} spaces in directory
  section#open
    h1 Open
    ul
  section#tweets
    h1 Tweets
    ul
  section#spaces
    h1 Spaces
    ul
  section#loading
    h1 API Status
    ul
      each url, name in directory
        li(id='#{name}')
          details
            summary #{name}
            a(href='#{url}') #{url}
            ul
  section#about
    h1 About
    :markdown
      Various [hackerspaces](http://en.wikipedia.org/wiki/Hackerspace) from
      around the world implement the
      [HackerSpace Status API](http://hackerspaces.nl/spaceapi/) and make
      the endpoint available on the web.
      This API is designed to make information about the space available
      for consumption by other software.
      
      This page uses a directory of such endpoints and combines information
      in various ways to produce the above content.
